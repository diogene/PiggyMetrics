spring:
  application:
    name: statistics-service
  cloud:
    config:
      uri: http://${CONFIG_SERVER:config}:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user